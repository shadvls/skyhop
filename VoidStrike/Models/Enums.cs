namespace VoidStrike.Models;

public enum GameStatus
{
    Menu,
    Playing,
    Paused,
    GameOver,
    LevelComplete
}

public enum AsteroidSize
{
    Large = 40,
    Medium = 20,
    Small = 10
}
